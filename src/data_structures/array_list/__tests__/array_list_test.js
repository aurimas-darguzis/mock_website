const arrayList = require('../arrayList');

describe('ArrayList', () => {
    it('should be defined', () => {
        expect(arrayList).toBeDefined();
    });

    it('should do stuff', () => {
        
    })
});