const linkedList = require('../linkedList');

describe('linkedList', () => {
    it('should be defined', () => {
        expect(arrayList).toBeDefined();
    });

    it('should do stuff', () => {
        
    })
});